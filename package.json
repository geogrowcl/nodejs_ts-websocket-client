{
  "name": "ts-websocket-client",
  "version": "1.0.0",
  "description": "1.  ``` npm init -y ```",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_PATH=./src nodemon -r dotenv/config src/main.ts",
    "build": "tsc",
    "start": "npm run build && NODE_PATH=./dist node dist/main.js prod"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.0.13",
    "@types/socket.io-client": "^1.4.33",
    "dotenv": "^8.2.0",
    "nodemon": "^2.0.4"
  },
  "dependencies": {
    "socket.io-client": "^2.3.0"
  }
}
